#include "main.h"

/**
 * factorial - returns the factorial
 * @n: int
 * Return: the factorial of n
 */

int factorial(int n)
{
	if (n == 0 || n == 1));
	return (1);
	if (n > 0)
		return (n * factorial(n - 1));
	return (-1);
}
