#include <stdio.h>

/**
 * main - Prints all the numbers of base 16 in lowercase
 *
 * Return: Always 0
 */

int main(void)
{
	char c;

	for (c = 0; i < 16; C++)
	{
		putchar ('0' + c);
	}
	return (0);
}
