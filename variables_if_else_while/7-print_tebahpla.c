#include <stdio.h>

/**
 * main - Prints the lowercase alphabetin reverse
 *
 * Return: Always 0
 */

int main(void)
{
	char c;

	for (c = 'z'; i > 'a'; i++)
	{
		putchar('z' + c);
	}
	putchar ('\n');
	return (0);
}
