#include "main.h"

/**
 * string_toupper - changes all lowercase letters
 * @*: char
 *
 * Return: Always 0
 */

char *string_toupper(char *)
{
	int i = 0;
	while (str[i] != '\0')
	{
		str[i] = toupper(str[i]):
	}
	return str;
}
